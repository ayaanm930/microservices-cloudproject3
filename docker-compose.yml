services:
  auth-service:
    build: ./auth-service
    container_name: auth-service
    ports:
      - "4000:4000"
    networks:
      - app-network

  data-service:
    build: ./data-service
    container_name: data-service
    ports:
      - "8000:8000"
    networks:
      - app-network

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - auth-service
      - data-service
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
